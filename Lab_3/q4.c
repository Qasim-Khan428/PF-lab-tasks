#include <stdio.h>
int main() {
    float Temperature;
    printf("Enter the temperature in Celsius: ");
    scanf("%f", &Temperature);
    printf(" Temperature: %.2f", Temperature);
    return 0;
}
